function [mu, sigma] = updateMu(Pi0, Piz, n_topic, n_location, n_doc_zi, n_doc_z, optimFunc, options, penalty_delta_i0, penalty_mu_iz)

%% update mu.

%     tic
%     fprintf('maximize Pi0\n');
func = @(x)negLogLikelihoodMuFunc(x, Piz, n_topic, n_location, n_doc_zi, n_doc_z);
x0 = Pi0;
Pi0 = optimFunc(func,x0,penalty_delta_i0*ones(size(x0)),options);
%     toc

